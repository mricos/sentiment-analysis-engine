DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )";
APP="$(basename $(dirname $DIR))";
PORT=$(cat $DIR/port);
PID=$(cat $DIR/pid);
[ ! -z $pid ] && STATUS="Running" || STATUS="Not running"

echo "Role: $USER
Application: $APP
PID: $PID
Status: $STATUS
Port: $PORT"

